# Zenno Booking

Zenno's booking and commerce application for appointment-based businesses.

## Local development

```bash
npm install
npm run dev
```

Brand URLs can be overridden with `VITE_PUBLIC_SITE_URL`,
`VITE_PUBLIC_APP_URL`, `VITE_PUBLIC_SUPPORT_URL`, and
`VITE_PUBLIC_DOCS_URL`.
